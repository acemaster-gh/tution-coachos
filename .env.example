# Copy to .env.local and set a real random value before deploying anywhere
# beyond your own machine. Without this, auth falls back to an insecure
# dev-only secret (see src/lib/auth.ts).
SESSION_SECRET=
